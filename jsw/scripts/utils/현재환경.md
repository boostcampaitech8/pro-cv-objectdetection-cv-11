GPU 개수: 1개

GPU 메모리: 32GB (Tesla V100-SXM2)

CUDA 버전: 12.2

V100 32GB 기준으로 DETR + ResNet50 backbone + Albumentations 증강 사용 시

안전하게 batch_size=4~8 가능

Albumentations RandomResizedCrop(1024) 때문에 메모리 부담 있음 → batch_size=4 권장

GPU 1개이므로 effective batch size = 4


3️⃣ Learning Rate 추천

원래 config: lr=1e-4 (batch_size 16 기준)

지금 effective batch size = 4 → LR 비례로 줄여야 함 (linear scaling)
따라서 LR = 2.5e-5 추천